/***  CELIS, KRISTINA | ASSIGNMENT 1 | lec1_c2  ***/

#include <stdio.h>

int main(void)
{

    int minuend, subtrahend, difference;    // Declaring variables as integers

    minuend = 87;                           // Assigning the values to the variables declared
    subtrahend = 15;
    difference = minuend - subtrahend;      // Formula for subtracting

    // the statement below will print the output (message, integers and difference) in the terminal. 
    printf("Subtracting %d from %d is equal to a difference of %d\n", subtrahend, minuend, difference);

    return 0;
}
